Live card: `.harness/task/card.md` (gitignored). Regenerated by `npx tsx scripts/record-this-slice.ts`, which **spawns** Vitest and records raw output. Handwritten passed checks are rejected.

Snapshot of the 20 Sep 2026 provenance fix: 9/10 criteria verified by executed processes; `tui-live` not run; agreement proposed; owner accepted no.

See `docs/task-agreement.md`. Confirm with `/task confirm`. Accept with `/task accept`.
