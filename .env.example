# Jev — spend/danger lock
TYPESAFE_API_KEY=
# or
TYPESAFE_AI_API_KEY=

# Chat model — OpenCode Zen (preferred). Paste when you have it.
OPENCODE_API_KEY=
OPENCODE_BASE_URL=https://opencode.ai/zen/v1
# Omit a pin so Jev routes cheap vs frontier. Pin with --model or /model <id>.
GATE_CHEAP_MODEL=glm-5.3-flash
GATE_FRONTIER_MODEL=glm-5.3

# Optional fallback
OPENAI_API_KEY=
